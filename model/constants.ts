export enum LocalStorageKeys {
   favoriteLaunches = "favoriteLaunches",
   favoriteLaunchPads = "favoriteLaunchPads",
   favoriteMissions = "favoriteMissions",
   favoriteShips = "favoriteShips",
   defaultIndex = "defaultIndex"
}

export enum SpaceXSections {
   launches = "launches",
   launchPads = "launchPads",
   missions = "missions",
   ships = "ships",
}

export const iconSize = 20

export const launchesPageSize = 5
export const launchPadPageSize = 8
export const missionPageSize = 8
export const shipPageSize = 8